{
  "name": "moonlit-flamingo-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bun run scripts/build.ts",
    "build:watch": "bun run scripts/build.ts --watch"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.323",
    "esbuild": "^0.25.0"
  }
}
